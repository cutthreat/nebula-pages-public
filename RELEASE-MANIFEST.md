# Nebula public static release — 2026-09-14

This is the single GitHub Pages payload for the current release. It contains Home, fourteen `nebula-easy-*` landing pages, and `nebula-account` as expanded static HTML with its runtime assets.

Open these files after publication:

- `home.html`
- `nebula-easy-psychic-reading.html`
- `nebula-account/psychics.html`
- `nebula-account/chatroom.html`

Excluded deliberately: historical layouts, `experiments/`, Yii2 runtime/cache, generated browser assets, video recordings, QA-heavy artifacts, and `nebula-account/source` Figma PNG references. They are not deploy inputs. The source handoff branch retains the Yii2 package, Figma references and compare materials.
