# Tarot Reading source-rebase — terminal closeout

Status: `not_pass_visual_source_fidelity`; no Caster, Anton, Yii2, donor, promotion, or deploy gate was started.

## Performed bounded static pass

- Rebound the existing isolated `nebula-easy-tarot-reading.html` to the complete source-backed legacy Tarot DOM/cascade under its own URL by a local `_unzipped/` base. The legacy route and all protected pages/files were not changed.
- Fresh C76/live postimages and runtime proof cover `1200/992/768/576/320`.
- Runtime is PASS: HTTP 200, six advisor cards and six FAQ controls at every width, no horizontal overflow, no console/local-request errors, and menu open/close PASS at 768/576/320.

## Visual decision

`1200`, `992`, `768` have exact source/live dimensions. `576` and `320` do not, so the candidate cannot pass five-width source fidelity.

First exact residual: at `576`, the local C76 source raster and manifest declare `576x8740`, while direct exact Figma context for root `893:12903` has its final footer at `7960 + 680 = 8640px`; the live source-rebound page is `8649px`. This is a material source conflict, not a safe CSS target. A repair would require inventing approximately 91px of geometry. `320` retains a separate `17px` height delta (`9995` source versus `9978` live).

No narrow repair was applied because there is no single authoritative target for the 576 residual. The unit stops here rather than fitting CSS to a contradictory export.

## Proof refs

- `SOURCE-RECEIPT.md`
- `post-runtime-proof.json`
- `visual-comparison.json`
- `post-{1200,992,768,576,320}.png`

Preview: `http://127.0.0.1:8765/nebula-easy-tarot-reading.html`.
